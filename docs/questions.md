does every module need to be in node.js fastify if we choose that module?
can we write modules in any language that works, unless specified in the module?

multiplayer on one mashine (one keyboard) with guest user nickname?

what are microservices in web development?
